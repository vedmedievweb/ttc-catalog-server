{
	"version": 2,
	"builds": [
		{
			"src": "index.ts",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/api/(.*)",
			"dest": "/dist/index.js"
		}
	],
	"installCommand": "npm install",
	"buildCommand": "npm run build"
}
